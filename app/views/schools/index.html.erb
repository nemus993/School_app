
<h1>Listing Schools</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @schools.each do |school| %>
    <%= school.title %></br>
    <%= link_to "Details", school_path(school.id) %>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New School', new_school_path %>
